---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: victoria-logs-vector
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: victoria-logs-vector
  values:
    controllers:
      victoria-logs-vector:
        type: daemonset

        annotations:
          reloader.stakater.com/auto: "true"

        pod:
          hostNetwork: true
          dnsPolicy: ClusterFirstWithHostNet
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534

        containers:
          app:
            image:
              repository: ghcr.io/vectordotdev/vector
              tag: 0.53.0-distroless-libc@sha256:ef46e576bdc6b1da34e11efda5b5d39611124713846e6ed97ef3fa6acf2ec522
            args:
              - --config
              - /etc/vector/vector.yaml
            env:
              NODE_NAME:
                valueFrom:
                  fieldRef:
                    fieldPath: spec.nodeName
            resources:
              requests:
                cpu: 10m
                memory: 32Mi
              limits:
                memory: 128Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL

    persistence:
      config:
        type: configMap
        name: victoria-logs-vector
        globalMounts:
          - path: /etc/vector/vector.yaml
            subPath: vector.yaml
            readOnly: true
      data:
        type: emptyDir
        globalMounts:
          - path: /var/lib/vector
