---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmproxy_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMProxy
metadata:
  name: litellm
spec:
  # renovate: datasource=docker depName=ghcr.io/berriai/litellm
  image: ghcr.io/berriai/litellm:v1.99.0@sha256:570a872d2fde8f1bc4a147634810941103c14697270f0f2918c5aa7d8201cac5
  replicas: 1
  livenessProbe:
    httpGet:
      path: /health/liveliness
      port: 4000
    initialDelaySeconds: 30
    periodSeconds: 15
    failureThreshold: 20
  generalSettings:
    health_check_endpoint: /v1/health
  routerSettings:
    routing_strategy: simple-shuffle
    redis_host: os.environ/REDIS_HOST
    redis_port: os.environ/REDIS_PORT
    num_retries: 3
    timeout: 600
    fallbacks:
      # direct same-model failover
      - opencode-go/glm-5.2: ["zai-org/glm-5.2"]
      - opencode-go/kimi-k2.6: ["moonshotai/kimi-k2.6"]
      - opencode-go/minimax-m3: ["minimaxai/minimax-m3"]
      # cross-family failover (lanes Lilac has no direct twin for)
      - opencode-go/kimi-k2.7-code: ["moonshotai/kimi-k2.6"]
      - opencode-go/mimo-v2.5: ["moonshotai/kimi-k2.6"]
      - opencode-go/qwen3.7-max: ["zai-org/glm-5.2"]
      - opencode-go/qwen3.6-plus: ["zai-org/glm-5.2"]
      - opencode-go/deepseek-v4-flash: ["moonshotai/kimi-k2.6"]
  litellmSettings:
    success_callback:
      - prometheus
    prometheus_adapter: true
    cache: true
    cache_params:
      type: redis
      host: os.environ/REDIS_HOST
      port: os.environ/REDIS_PORT
    drop_params: true
    merge_reasoning_content_in_choices: true
  env:
    - name: LITELLM_LOG
      value: INFO
    - name: LITELLM_MODE
      value: PRODUCTION
    - name: REDIS_HOST
      value: litellm-dragonfly.ai.svc.cluster.local
    - name: REDIS_PORT
      value: "6379"
    - name: LITELLM_MASTER_KEY
      valueFrom:
        secretKeyRef:
          name: litellm
          key: LITELLM_MASTER_KEY
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 2Gi
  service:
    type: ClusterIP
    port: 4000
  route:
    hostnames:
      - litellm.negative7.com
    parentRefs:
      - name: envoy-internal
        namespace: network
